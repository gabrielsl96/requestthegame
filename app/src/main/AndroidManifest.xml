<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.request_thegame">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--Api do google-->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/api_key" />

        <service android:name=".Services.ServiceListenerDesafio"/>

        <activity android:name=".Activ.desafios.DesafioQuatroActivity" />
        <activity android:name=".Activ.desafios.DesafioTresActivity"/>
        <activity android:name=".Activ.desafios.DesafioDoisActivity" />
        <activity android:name=".Activ.desafios.DesafioUmActivity" />

        <activity android:name=".Activ.CriarDesafiosActivity">
        </activity>
        <activity android:name=".Activ.PainelDesafiosActivity"
            />
        <activity android:name=".Activ.MainActivity"
            android:theme="@style/AppTheme.StatusBarDark">
        </activity>
        <activity android:name=".Activ.LoginActivity">
        </activity>
        <activity android:name=".Activ.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>